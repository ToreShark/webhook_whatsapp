events {
    worker_connections 1024;
}

http {
    upstream whatsapp_webhook {
        server whatsapp-webhook:3000;
    }

    server {
        listen 80;
        server_name whatsapp.webhook.primelegal.kz;

        location /.well-known/acme-challenge/ {
            root /var/www/html;
        }

        location / {
            # Temporarily proxy HTTP requests until SSL is fixed
            proxy_pass http://whatsapp_webhook;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    # HTTPS server temporarily disabled - certificates need to be mounted
    # Uncomment after mounting certificates volume
    # server {
    #     listen 443 ssl http2;
    #     server_name whatsapp.webhook.primelegal.kz;

    #     ssl_certificate /etc/letsencrypt/live/whatsapp.webhook.primelegal.kz/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/whatsapp.webhook.primelegal.kz/privkey.pem;

    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384;
    #     ssl_prefer_server_ciphers off;

    #     location / {
    #         proxy_pass http://whatsapp_webhook;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    # }
}